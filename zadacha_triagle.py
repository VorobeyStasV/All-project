class TriangleCheker:
    def __init__(self, storona_A, storona_B, storona_C):
        self.storona_A = storona_A
        self.storona_B = storona_B
        self.storona_C = storona_C
    def is triagle(self):
        if all(isistanca(sied, (int, float)) for side in self.sides):
            if all(side > 0 for side in self.sides)
                sorted_sides = sorted_sides[1] > sorted_sides[2]:
                    return"ура"
                return"sorry"
            return"otric symbols no"
        return"only symbols"






        storona_A >= storona_B <= storona_C and storona_A <= storona_B >= storona_C:
            self.storona_A = storona_A
            self.storona_B = storona_B
            self.storona_C = storona_C
    else:
        self.TriangleCheker
    def is_triagle(self):
        if self.TriangleCheker:

is_triagle = ("")
is_triagle1 = soda("malina")
is_triagle2 = soda(5)
is_triagle1.show_my_drink()
is_triagle2.show_my_drink()
is_triagle3.()